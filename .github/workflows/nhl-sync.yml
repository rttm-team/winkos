name: Nightly NHL Stats & Scoring Sync

on:
  schedule:
    # Runs every night at 3:00 AM EST (08:00 UTC)
    - cron: '0 8 * * *'
  workflow_dispatch: # Allows manual trigger button in GitHub Actions UI

jobs:
  sync-stats:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run NHL Sync & Scoring Script
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: python scripts/nhl_sync.py

      - name: Sync Active Roster Stats
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: python scripts/sync_active_roster.py
